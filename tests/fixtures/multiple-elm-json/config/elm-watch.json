{
  "postprocess": ["node", "postprocess.js"],
  "targets": {
    "app": {
      "inputs": ["../app/Main.elm"],
      "output": "../build/app.js"
    },
    "admin": {
      "inputs": ["../admin/Main.elm"],
      "output": "../build/admin.js"
    }
  }
}
