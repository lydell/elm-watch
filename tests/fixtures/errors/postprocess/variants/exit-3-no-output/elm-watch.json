{
  "postprocess": ["node", "-e", "process.stdin.resume(); process.stdin.on('end', () => { process.exit(3) })"],
  "targets": {
    "main": {
      "inputs": ["../../src/Main.elm"],
      "output": "../../build/exit-3-no-output.js"
    }
  }
}
