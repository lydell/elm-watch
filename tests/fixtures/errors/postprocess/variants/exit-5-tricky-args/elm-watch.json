{
  "postprocess": ["node", "-e", "process.stdin.resume(); process.stdin.on('end', () => { process.exit(5) })", "--", "", "'a'b'", "$x"],
  "targets": {
    "main": {
      "inputs": ["../../src/Main.elm"],
      "output": "../../build/exit-5-tricky-args.js"
    }
  }
}
