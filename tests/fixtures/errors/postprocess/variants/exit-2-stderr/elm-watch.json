{
  "postprocess": [
    "node",
    "-e",
    "process.stdin.resume(); process.stdin.on('end', () => { console.error('some stderr'); process.exit(2) })"
  ],
  "targets": {
    "main": {
      "inputs": ["../../src/Main.elm"],
      "output": "../../build/exit-2-stderr.js"
    }
  }
}
