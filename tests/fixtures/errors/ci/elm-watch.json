{
  "postprocess": ["node", "-e", "process.stdin.resume(); setTimeout(() => process.exit(1), 50)"],
  "targets": {
    "admin": {
      "inputs": ["src/Admin.elm"],
      "output": "build/admin.js"
    },
    "app": {
      "inputs": ["src/App.elm"],
      "output": "build/app.js"
    },
    "postprocess-error": {
      "inputs": ["src/App.elm"],
      "output": "build/postprocess-error.js"
    }
  }
}
